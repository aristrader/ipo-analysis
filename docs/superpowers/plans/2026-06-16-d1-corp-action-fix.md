# D-1 Corp-Action Fix Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

## Status / review rounds (living state record — newest at bottom)
- **R1 review** (`docs/research/d1_plan_review_2026-06-17.md`) — found F1 (direction-normalize the detector; drop PATANJALI guardrail → KAUSHALYA), F2 (permanent loud envelope tripwire), F3 (override contradiction guard), F4 (NPST out of magnitude parametrize), F5 (Task 7 → `mae_1y`), F6 (full anti-join byte-identity gate) + line-ref nits. **Folded ✅** (prior revision).
- **R2 re-review** (`docs/research/d1_plan_rereview_2026-06-17.md`) — found the symbol-join blockers R1 (ROLEXRINGS test mis-keyed to `INE645S01016`; must key price on substrate `INE645S01024` + symbol so the real ×1000 path is exercised) + R2 (the detector / Task-4 reconcile / Task-8 patch must join by `actions_for` **symbol∪substrate-ISIN**, not corp-action ISIN — blast radius 282 symbols / 434 rows) + nits R3 (duplicate Task-3 Step 6), R4 (share one gap helper), R5 (spell out which `substrate_meta.json` fields move). **Folded ✅** (this revision).
- **R3 join-strategy** (`docs/research/d1_join_strategy_2026-06-17.md`) — verdict = option **(d)**: join key = **symbol ∪ substrate-ISIN, GATED by the trading-date window** (`ex_date ∈ [first_trade, last_trade]`, small pre-listing slack); out-of-window matches DROPPED + flagged `corp_action_out_of_window` (→ unresolved set). This removes the 44 wrong-era/reused-symbol attachments (37 yfinance, 25 with rf<1) while keeping the 458 legitimate ones, and fixes the CAUSE (not just the envelope-tripwire symptom). Applied to the detector, reconcile, Task-8 patch, AND `07`'s `actions_for`/`load_prices`. **Folded ✅** (this revision). *(SCOPE CORRECTED by R6/D1-F1 below: the gate is symbol-added-only — ISIN-matched actions always apply; the real OOW union count is 86, of which only the symbol-added subset (~44) is droppable; and the gate is at the `07` CALL SITE, not inside `load_prices`, per R6/D1-F2.)*
- **R4 final verification** (`docs/research/d1_plan_final_verify_2026-06-17.md`) — **VERDICT: APPROVE-WITH-NITS → EXECUTION-READY.** All 7 checks confirmed against on-disk ground truth (join key in all 4 loci, R1 test fails for the right reason, F1–F6 intact, KAUSHALYA ~100× gap confirmed, no residual ISIN-keying). **3 BUILD-TIME nits** (not plan blockers): N1 — apply the window gate INSIDE the consumed helper (the trading window is only knowable after the CSV read inside `load_prices`), not "before load_prices" — **NOTE: R6/D1-F2 SUPERSEDES this — gate at the CALL SITE, not inside `load_prices`** (see below); N2 — there are TWO `actions_for`/`load_prices` call sites in `07` (~599-600 and ~630-631), so gating at both call sites makes both inherit it; N3 — cosmetic state-log framing. **No further plan revision required** *(superseded by R5/R6).*
- **R5 implementability** (`docs/research/d1_final_implementability_2026-06-17.md`) — **VERDICT: CONCRETE-WITH-NITS.** Every file/line ref, named artifact, and key value checks out against on-disk ground truth; an engineer with zero context CAN execute it. Two structural gaps + nits folded: **F-7 (MEDIUM, structural)** — three tasks (4/5b/6) set `corp_action_out_of_window` / `corp_action_unresolved` / `corp_action_envelope_violation` flags but NO column carries them from `07` → `returns_summary.csv` → `09` → `ipo_analysis.csv`; define the carrier column(s) in `07.columns()` and plumb through `09`'s `out_cols` (they flow via `ret_keys`). **F-1 (MEDIUM)** — Task 1 Step 4/5 imports an unbuilt `detect_gap` (Task 2) → reorder/note the dependency so the green baseline is reachable. **F-3 (MEDIUM)** — `detect_gap`'s window is **series-INDEX-adjacent, NOT calendar** (the real ROLEXRINGS gap spans a 15-month hole, KAUSHALYA a ~3.5-week suspension — calendar proximity silently misses them). **F-2 (LOW)** — `applied_effective` for a multi-event product = `max(P, 1/P)`. **F-4 (MEDIUM)** — pin the ROLEXRINGS override seed literally + parse the evidence CSV with quoted `csv.DictReader` (comma-bleed). **F-8 (MEDIUM)** — enumerate WHICH outcome columns Task 6 nulls (don't leave "~80 candidates" implicit). **Folded ✅** (this revision).
- **R6 final correctness** (`docs/research/d1_final_correctness_2026-06-17.md`) — **VERDICT: FLAW-FOUND** (one HIGH, one MEDIUM, plus a test-safety gap + nits). Whole-system, per-bucket correctness gate. **D1-F1 (HIGH — would-be regression):** the date-window gate is **over-scoped** — applying it to the WHOLE union (incl. ISIN-matched actions) wrongly drops **26 legitimate ISIN-matched post-listing, pre-price-coverage splits** (the daily-bhavcopy price file starts ~2017 for old stocks). An **ISIN-matched** action is the stock's OWN corporate event and must **ALWAYS apply** (subject to price-validation); only **symbol-added** matches (the reuse-risk ones) get the trading-window gate. Real OOW count = **86 union actions** (the prior "44" counted symbol-added only). Concrete regressions the over-scoped gate would inject: **ATLANTAA** (2010 5:1 split → winner-flips-loser), **TARIL** (multibagger label at risk), **ADANIPORTS / TITAGARH / BAJAJCON** (5× adj_issue error). **D1-F2 (MEDIUM):** gate at the **CALL SITE, not inside `load_prices`** — the `actions` list feeds `load_prices` (series), `adj_factor_after` for issue→adj_issue (`07:438`), AND terminal (`07:270`); filtering only inside `load_prices` desyncs issue/terminal from the series. Build the windowed `actions` list once at the call site and pass the SAME list to both `load_prices` and `compute()`. **D1-M1 (MEDIUM):** the full anti-join byte-identity (F6) must compare only the **PRE-EXISTING column set** (the new `corp_action_*` columns re-serialize every row → false fail). **D1-N1 (NIT):** add a test pinning the compound same-day split+bonus (ISHAN, 10×3=30 vs gap 28.56 — reconcile must keep the combined factor, not drop a leg). **Folded ✅** (this revision). *(Design unchanged — price-disposes, flag+null, genuine-wipeout=−100%, confidence invariant, targeted-patch all intact; only the gate scope/locus, the carrier column, the window semantics, and the byte-identity comparison were corrected.)*
- **R7 targeted re-verify** (`docs/research/d1_plan_targeted_reverify_2026-06-17.md`) — **VERDICT: GO (EXECUTION-READY).** Focused check of the D1-F1 gate-scoping fix + the related folds (D1-F2/F-7/D1-M1). All 6 items confirmed on disk: gate is symbol-added-only at every locus & ISIN-matched always applies (ATLANTAA/TARIL/ADANIPORTS/TITAGARH/BAJAJCON kept — adj_issue stays correct); the 44 reuse/wrong-era drops hold; KAUSHALYA's genuine reverse split is in-window (not a new casualty); D1-F2 call-site locus correct; F-7 carrier-column plumbing real (`07.columns`→`09 ret_keys`→substrate); D1-M1 anti-join over pre-existing columns; supersession notes coherent. **No blocker. Plan CONVERGED — pre-review STOPS here; build is gated only on owner go.** *(SUPERSEDED by R8 — R7 was a targeted check of the gate fix, did not re-examine whether price-validation reaches the substrate.)*
- **R8 adversarial edge-case hunt** (`docs/research/d1_adversarial_2026-06-17.md`) — **VERDICT: EDGE-CASE-FOUND (HIGH, build-blocking).** "Price disposes" does NOT reach the substrate: the price-validating reconcile (`03k`/`03l`, Task 4) is DAG-orphan and never regenerates `corp_actions_merged.csv`; the substrate path is Task 8 → `07.compute()`, and `07` reads `corp_actions_merged.csv` directly + applies the **window gate ONLY, NO gap check** (`07:206-246`). So **in-window phantoms / same-event double-counts survive on NON-seeded stocks** — CANTABIL (factor 25 not 5 → 39.68× vs correct 7.14×), GNA (factor 4 → 6.39× vs 2.70×), RPEL/VISHWARAJ/GICL/PAVNAIND/KNRCON/ASIANTILES (~13–tens of rows). Envelope tripwire is provably BLIND to uniform over-counts. **REMEDY (rec (a)):** add per-action price-validation (`detect_gap`) at the `07` call site / substrate-reaching path — drop/collapse any admitted leg with no supporting gap; (alt (b): un-orphan `03k`/`03l` so Task 8 consumes the reconciled file). **NOT execution-ready until folded.** (Clean: ISIN-matched-always-apply, direction-norm, slack, flag-union.) *(SUPERSEDED by R9 — remedy (a) folded.)*
- **R9 adversarial remedy (a) folded** (this revision) — **price-validation now REACHES the substrate path.** Per the R8 break, "price disposes" was confined to the DAG-orphaned `03k`/`03l` reconcile and never ran on `07.compute()` (the only substrate-reaching path via Task 8). Remedy (a) folded: **per-action price-validation is now applied at the `07` CALL SITE** (new Task 4 Step 6b, immediately AFTER the symbol-added window gate of Step 6, BEFORE the windowed `actions` list is passed to `load_prices`/`compute`). For each admitted action, `detect_gap` (Task 2) decides: gap-supports-ratio → **apply**; multiple same-ratio legs → ONE price gap → **collapse to one** (the double-count fix — CANTABIL 25→5, GNA 4→2); no gap but **price present** → **PHANTOM → DROP + flag `corp_action_no_price_gap`** → unresolved; no gap because **data missing** (coverage hole) → **defer to the override table** (apply if a confident override exists; else flag-unresolved — ROLEXRINGS stays resolved via seed). The substrate-reaching enforcement is THIS `07`-path validation, NOT the offline reconcile — so the substrate is correct even if `03k`/`03l` stay orphaned. Tests pin CANTABIL (→~7.14×, NOT multibagger) + GNA (→~2.70×), assert the envelope-tripwire-blind uniform-over-count class is now caught by the price-validation, and preserve the phantom-drop-vs-coverage-hole-defer distinction (ROLEXRINGS not wrongly dropped). Tasks updated: 1 (detector reflects the validated path), 4 (call-site per-action validation), 6 (new phantom flag → unresolved set), 8 (patch inherits via `07.compute`). **Design unchanged** (price-disposes, flag+null, genuine-wipeout=−100%, confidence invariant, targeted-patch). **Pre-review STOPS here; a targeted re-verify of remedy (a) runs next.**
- **R10 remedy-(a) re-verify** (`docs/research/d1_remedy_a_reverify_2026-06-17.md`) — **VERDICT: GO (BUILD-READY).** All 6 items confirmed on disk: price-validation reaches the substrate (Step 6b at both `07` call sites, inherited by Task 8); CANTABIL collapses 25→5 (~7.14×, not multibagger) + GNA 4→2 (~2.70×) traced on real price files (GNA proves the mixed phantom-symbol/real-ISIN case); phantom-drop vs coverage-hole-defer correct (ROLEXRINGS protected via seed); no regression (ATLANTAA stays +36.3% winner); carrier column + envelope-blindness covered; no R9 contradiction. **1 LOW non-blocking nit (NIT-1, BUILD-TIME):** add an explicit 5th decision-tree branch — *gap PRESENT but doesn't match the claimed ratio (real ROLEXRINGS ~20× across a coverage hole vs claimed 10×) → defer to override* (distinct from literal "no gap / data-missing") — and use a REAL-ROLEXRINGS test, not a synthetic `None` fixture; behavior already correct, fold during the Task-4 build. **PLAN CONVERGED (10 rounds) — pre-review STOPS; BUILD gated on owner go (🚦 HELD until owner explicitly says go).** *(SUPERSEDED by R11 — NOT build-ready; see below.)*
- **R11 — 2nd adversarial, fresh focus** (`docs/research/d1_adversarial2_2026-06-17.md`) — **VERDICT: EDGE-CASE-FOUND → NOT BUILD-READY (supersedes R10).** Two new breaks: **B1 (HIGH)** — genuine small bonuses (5:4/6:5/4:3, gap ~1.2–1.4×) fall below `detect_gap`'s 1.5× ABSOLUTE floor → wrongly phantom-dropped + nulled — **23 substrate stocks incl POWERGRID/NTPC/PFC/RECLTD/RITES/OIL.** **B2 (MED)** — phantom-vs-coverage-hole undecidable when `ex_date` is PAST the price-coverage end (closes before ex, none at/after) — 6 events (E2E/FORGE/LEMERITE, multibaggers) → could null real multibaggers or keep unvalidated 10×. **CLEAN (verified):** action-type confusion (no dividend/rights factor events; type never branched), face-value-change ISIN join, same-ratio-different-event (29 stocks each keep their own gap, not collapsed). **ROOT CAUSE — B1 + B2 + R10-NIT-1 all stem from ONE thing:** `detect_gap` uses an ABSOLUTE 1.5× threshold and its `None` return OVERLOADS ≥4 meanings (genuine-small / phantom-flat / coverage-missing / past-coverage-boundary / wrong-ratio). **FIX (root, not whack-a-mole): redesign `detect_gap` (Task 2) to be RATIO-AWARE — accept iff the measured gap ≈ the CLAIMED ratio (so a 1.25× gap validating a 5:4 bonus is ACCEPTED), not an absolute floor — and return a STRUCTURED result (matched / flat-phantom / missing / past-boundary); then rebuild the Task-4/Step-6b decision tree around the structured result.** Resolves B1+B2+NIT-1 together. Add tests: POWERGRID-class small bonus (kept), E2E/FORGE past-boundary (coverage-hole branch). *(Low data note: DWARKESH Rs10→Re1 mis-recorded rf=1.0 under-count — inert, not in substrate — log to TODO-D1.)* **NEXT: fold this root redesign → targeted re-verify → THEN build.**

**Goal:** Stop corporate-action splits/bonuses from being over-/mis-counted by validating every adjustment against the raw price gap ("price disposes"), so fake returns (ROLEXRINGS +15,272%) disappear while genuine large events (KAUSHALYA, a real 1:100 reverse split that gapped ~×100) stay intact.

**Architecture:** Rebuild corp-action reconciliation (`03k`/`03l`, consumed by `07`) around **direction-normalized** price-validation, joined by the **symbol∪substrate-ISIN key with a trading-date window applied ONLY to the symbol-added subset** (join-strategy option (d), R3, scoped by R6/D1-F1 — `ex_date ∈ [first_trade, last_trade]`; out-of-window SYMBOL-ADDED matches dropped+flagged `corp_action_out_of_window`, fixing the CAUSE of the symbol-reuse/wrong-era attachments — while **ISIN-matched actions ALWAYS apply** since they are the stock's own event); auto-derive the factor from the price gap (measured on a **series-index-adjacent**, not calendar, window) with a tiny override table (+ a contradiction guard) for price-ambiguous cases. **(R9 — remedy (a)) The "price disposes" validation runs PER ADMITTED ACTION at the `07` CALL SITE (the substrate-reaching path), not only in the DAG-orphaned `03k`/`03l` reconcile:** after the symbol-added window gate, each admitted action is checked against `detect_gap` — gap-supports → apply; multiple same-ratio legs mapping to ONE gap → collapse-to-one (the double-count fix, CANTABIL/GNA); no gap but price PRESENT → phantom → DROP + flag `corp_action_no_price_gap` → unresolved; no gap because data MISSING (coverage hole) → defer to the override table (never wrongly drop ROLEXRINGS). The substrate-reaching enforcement is the `07`-path validation; `03k`/`03l` are the offline source-of-truth but stay orphaned. **Define + plumb the `corp_action_*` flag carrier column(s)** end-to-end (`07.columns()` → `09` `ret_keys` → `ipo_analysis.csv`, F-7; incl. the new `corp_action_no_price_gap`); add a flag-only continuity guard AND a permanent flag-only envelope tripwire (`trough ≤ endpoint ≤ peak`); flag+null genuinely-unresolved rows (keeping genuine wipeouts at −100%); apply the correction as a **targeted patch** (reusing `07`'s compute path, which now carries the window gate AND the per-action price-validation) to the existing (non-reproducible) substrate, snapshot + full-anti-join-diff (over the **pre-existing column set**, D1-M1) first.

**Tech Stack:** Python 3.14, pandas/stdlib csv, pytest, the repo's numbered `pipeline/` steps + `data/master/` substrate. Source spec: `docs/superpowers/specs/2026-06-16-d1-corp-action-fix-design.md`.

**Brownfield rule:** every implementation task begins by READING the named file region to get exact current signatures before editing — do not assume function names. Run `source .venv/bin/activate` once per session; tests run `PYTHONPATH=. pytest`.

**⚠️ CONFIDENCE INVARIANT (owner, non-negotiable — applies to EVERY task that writes data):** write a FINAL corrected
value ONLY for a stock we can **confidently** resolve — i.e. the price gap validates the factor, OR a confident override
exists (Task 3). For ANY stock we cannot confidently resolve (ambiguous/missing price gap AND no reliable override),
**never write a guessed value** — route it to the *unresolved set* → flag + null the outcome (keep the row). The default
for uncertainty is **flag+null, never best-guess**. The unresolved set is **dynamic**: it is the 6 known Wave-1
STAYS-FLAGGED stocks PLUS any stock the Task-4 rebuild routes to the flag channel PLUS any stock with a
`corp_action_out_of_window`-dropped action (the date-window gate, R3, **symbol-added subset only** per D1-F1) PLUS
any stock with a **`corp_action_no_price_gap`-dropped PHANTOM action (the per-action price-validation, R9 remedy (a),
Task 4 Step 6)** PLUS any T-2 envelope residuals (Task 9) — ALL of them get flag+null, not just the named 6.

**⚠️ CANONICAL JOIN KEY (R3 / join-strategy option (d), scoped by R6/D1-F1 — applies EVERYWHERE a corp action is joined/grouped/keyed):**
the corp-action set for a substrate row = `by_isin[substrate-ISIN] ∪ by_symbol[NSE-symbol]` (the existing
`07.actions_for` union, mirroring `09:86-89`'s `has_action`).
- **ISIN-matched actions (`ex` ∈ `by_isin[substrate-ISIN]`) ALWAYS apply** — an ISIN-matched action is, by definition,
  the stock's OWN corporate event and must be adjusted **regardless of the price-coverage span** (the daily-bhavcopy
  price file starts ~2017 for old stocks, so a legitimate post-listing split that fell in the pre-coverage gap has
  `ex_date < first_trade` but is still real). They are subject only to the existing price-validation, NOT the window gate.
- **The trading-date window gate is scoped to the SYMBOL-ADDED subset ONLY** — actions reached via `by_symbol[NSE-symbol]`
  that are NOT also in `by_isin[substrate-ISIN]`. These are the reuse-risk ones (a freed NSE symbol re-assigned to a later
  listing attaches the OLD company's action). A symbol-added action is admitted ONLY if its `ex_date ∈ [first_trade,
  last_trade]` (with a small few-trading-day slack before `first_trade` for a listing-day split); an out-of-window
  symbol-added action is **DROPPED from adjustment and flagged `corp_action_out_of_window`** → routed to the unresolved set
  (per the Confidence Invariant) — NEVER applied.

**⚠️ D1-F1 (HIGH — DO NOT over-scope the gate):** applying the window gate to the WHOLE union (incl. ISIN-matched actions)
would wrongly drop **26 legitimate ISIN-matched post-listing, pre-coverage splits** and corrupt their issue-anchored
returns (the real OOW count is **86 union actions**; the prior "44" counted symbol-added only). Concrete regression-guard
cases the over-scoped gate would have broken — these MUST stay applied/unchanged by the patch:
**ATLANTAA** (`INE285H01022`, 5:1 ex 2010-11-08 → adj_issue 30.0, +36.3% **winner** — over-scoped gate flips winner→loser),
**TARIL** (`INE763I01026`, +1,434% **multibagger** label at risk), **ADANIPORTS** (`INE742F01042`, 5:1 ex 2010-09-23),
**TITAGARH** (`INE615H01020`, 5:1 ex 2015-04-23), **BAJAJCON** (`INE933K01021`, 5:1 ex 2011-05-05) — the last three would
get a 5× adj_issue error. Add a regression test pinning ATLANTAA winner-stays-winner + TARIL multibagger intact.

This scoping is the key the detector (Task 1), the reconcile (Task 4), `07`'s call-site gate (Task 4 — see D1-F2 below),
and the targeted patch (Task 8) must ALL use; keying on the corp-action-file ISIN alone is WRONG (R2 — 282 dual-ISIN
symbols / 434 rows reach prices only via the symbol join; 0 corp-action ISINs have their own price file).

**⚠️ D1-F2 (MEDIUM — gate at the CALL SITE, not inside `load_prices`):** the `actions` list feeds THREE consumers, not one
— `load_prices` (adjusts the price SERIES), `adj_factor_after` for `issue_price → adj_issue` (`07:438`), AND
`adj_factor_after` for the delisting `last_price → terminal` (`07:270`). Filtering only inside `load_prices` fixes the
series but leaves issue/terminal computed with the un-gated factor → an internal scale mismatch. **Build the windowed
`actions` list ONCE at each call site (~599-600, ~630-631) — read the window from the loaded/sorted prices, apply the
symbol-added-only gate — and pass the SAME filtered list to BOTH `load_prices` AND `compute()` (so `adj_factor_after`
inside `compute` sees it too).** "Wire into `load_prices`" alone is insufficient.

---

### Task 0: Branch + snapshot the substrate (safety first)

**Files:**
- Snapshot: `archive/pre_d1_<today>/` (copy of `data/master/`)

- [ ] **Step 1:** Create the work branch off `main`.
  Run: `git switch -c fix/d1-corp-action-overcounting`
- [ ] **Step 2:** Snapshot the substrate so we can diff/rollback (the substrate is NOT pipeline-reproducible).
  Run: `mkdir -p archive/pre_d1_$(date +%Y%m%d) && cp data/master/*.csv data/master/substrate_meta.json archive/pre_d1_$(date +%Y%m%d)/`
- [ ] **Step 3:** Confirm baseline state.
  Run: `PYTHONPATH=. python verify.py` → Expected: PASS. Run: `PYTHONPATH=. pytest tests -q 2>&1 | tail -3` → Expected: the known 12 pre-existing failures (T-2/T-3/T-4 + thinktank T-5) recorded as the baseline.
- [ ] **Step 4:** No commit needed — `archive/` is gitignored (`.gitignore` has `/archive/`), so the snapshot is **local-only rollback safety** (which is exactly what we want). Do NOT `git commit` the snapshot (it would be an empty/no-op commit). If you specifically want it tracked, use `git add -f archive/pre_d1_*` first — otherwise leave it untracked.

---

### Task 1: Over-adjustment detector — the failing test FIRST (proves the bug)

**Files:**
- Test: `tests/pipeline/test_corp_action_overadjust.py` (create)

The detector signature is **applied cumulative factor inconsistent with the observed raw price gap** — NOT raw magnitude (genuine 100:1 consolidations must pass). The comparison is **direction-normalized**: `applied_cumulative_factor` is a product of `ratio_factor`s and can be < 1 for a reverse split, while the observed gap is always measured ≥ 1. So we compare the **price-multiplier equivalent** `applied_effective` (= `1/ratio_factor` for a reverse split, = the applied factor for a forward split) against the gap in log-magnitude.

> **⚠️ R9 (remedy (a)) — the detector measures what the VALIDATED substrate path will actually apply.** As of R9 the `07`
> call site no longer applies the bare admitted-factor product: it price-validates each admitted action (gap-supports → apply;
> same-ratio legs mapping to one gap → collapse-to-one; price-present-no-gap → phantom-DROP; data-missing → override-or-flag).
> So `applied_cumulative_factor`/`applied_effective` here are the **pre-validation** product (they measure the over-count the
> bug produces — the RED baseline this test pins), while the post-fix substrate carries the **validated** factor (the GREEN
> assertion in Task 9). For CANTABIL/GNA the pre-validation product is the squared factor (25 / 4), the validated factor is the
> single real one (5 / 2). The detector + its `observed_max_gap` helper (which reuses `detect_gap`, Task 2) are the same gap
> math the call-site validation uses, so detector and substrate cannot disagree on what a clean event's factor should be.

> **⚠️ JOIN KEY (R1 + R2 + R3, scoped by R6/D1-F1 — non-negotiable for this task).** Key price/return/factor on the **substrate ISIN** (the post-split ISIN whose price file EXISTS), and obtain the applied factor via the **`actions_for` symbol∪substrate-ISIN union** (`by_isin[substrate-ISIN] ∪ by_symbol[symbol]`, dedup by `(ex_date, ratio_factor)`), NOT the corp-action-file ISIN. **Then apply the date-window guard to the SYMBOL-ADDED subset ONLY** (R3 + D1-F1): ISIN-matched events (`ex` ∈ `by_isin[substrate-ISIN]`) are the stock's own corporate events and are ALWAYS kept; only symbol-added events (in `by_symbol[symbol]` but NOT in `by_isin[substrate-ISIN]`) are required to satisfy `ex_date ∈ [first_trade, last_trade]` (small pre-listing slack). The factor product is computed over (all ISIN-matched events) ∪ (in-window symbol-added events) — so the detector measures what production *should* apply WITHOUT dropping a legit pre-coverage ISIN-matched split (the ATLANTAA/TARIL/ADANIPORTS regression class, D1-F1). For ROLEXRINGS: key on substrate **`INE645S01024`** (price file exists; there is **no** `INE645S01016.csv`) + symbol `ROLEXRINGS`; its three events all sit under `by_symbol["ROLEXRINGS"]` (symbol-added), all in-window → product **1000** → flagged for the RIGHT reason against the real ~20× gap (ratio ≈ 50). Keying on `INE645S01016` gives `applied=10`/`gap=None→1.0` → passes spuriously (test green on a lie). The detector must FAIL iff the over-count exists and exercise the REAL production path.

> **⚠️ BUILD-ORDER (F-1 — Task 2 before Task 1's Step 4/5).** Step 4's helper imports `pipeline.price_gap.detect_gap`, which Task 2 creates. An agent running strictly in task order will hit an ImportError and Step 5's "expect PASS" green baseline is unreachable until `price_gap` exists. **Either build Task 2 (`price_gap.detect_gap`) FIRST, then return here for Step 4/5, or stub the import and fill it once Task 2 lands.** Do not treat Step 5 as a stall.

- [ ] **Step 1: Read** `pipeline/07_returns_summary.py` to learn how the cumulative factor per ISIN is built and applied. **Correct loci (verified on `main`):** `load_corp_actions` builds `by_isin` + `by_symbol` = **91-111**; `actions_for` union helper = **114-127** (the per-ISIN/per-symbol event lookup, NOT the application); the factor is APPLIED in `load_prices` = **206-235** (price ÷ factor) and `adj_factor_after` = **239-246**. Also read `pipeline/03l_merge_corporate_actions.py` (the union/append is the `pd.concat` at **line 58**; 44-53 is the Yahoo→NSE row-shaping loop) to learn the merged event record shape and where prices live (`data/prices/<isin>.csv`).
- [ ] **Step 2: Write the failing test.** It loads the CURRENT merged corp actions + price files and asserts the over-adjustment is present for known offenders and absent for genuine events. The detector uses **`applied_effective` vs gap** (direction-normalized), so it is correct for forward AND reverse splits.

```python
# tests/pipeline/test_corp_action_overadjust.py
import math
import pytest

# applied_effective + observed_max_gap must be importable from the pipeline; this task
# adds the thin read-only helper module (see Step 4). The helper REUSES detect_gap from
# pipeline.price_gap (Task 2) for its window math (R4 — one gap helper, no drift).
from pipeline.corp_action_audit import applied_effective, observed_max_gap  # to be created

@pytest.mark.parametrize("isin,symbol", [
    # R1: key on the SUBSTRATE ISIN (price file exists; INE645S01016.csv does NOT).
    # The 3x over-count (product 1000) lives under by_symbol["ROLEXRINGS"], reached
    # via the actions_for union onto INE645S01024 — all three events in-window.
    ("INE645S01024", "ROLEXRINGS"),  # one 10:1 split counted 3x -> applied_effective ~1000
    # R9 (remedy (a)): in-window SAME-EVENT double-counts the window gate alone keeps.
    # Both legs in-window, only ONE price gap -> applied_effective is the SQUARED factor
    # (the RED baseline); the validated substrate collapses each to one (Task 9 GREEN).
    ("INE068L01024", "CANTABIL"),    # one 5:1 counted 2x -> applied_effective ~25 (vs gap ~5)
    ("INE934S01014", "GNA"),         # one 2:1 counted 2x -> applied_effective ~4 (vs gap ~2)
])
def test_known_overadjusted_stocks_flagged(isin, symbol):
    # direction-normalized: compare the price-MULTIPLIER equivalent to the observed gap.
    # Both helpers key on the (substrate-ISIN, symbol) union AND apply the date-window
    # guard, so they measure what production *should* apply (R2 + R3).
    eff = applied_effective(isin, symbol)   # = 1/rf for reverse, =applied for forward; in-window only
    max_gap = observed_max_gap(isin, symbol) or 1.0   # largest near-ex-date raw-close gap (in-window)
    # the bug: applied multiplier is wildly larger than any real single-day price gap
    assert abs(math.log(eff)) - abs(math.log(max_gap)) > math.log(5.0), \
        f"{symbol}: expected over-adjustment (applied_effective {eff} vs gap {max_gap})"
```

(The helper resolves the candidate event set via the `07.actions_for` union onto the substrate ISIN and reads the price window `[first_trade, last_trade]` from `data/prices/<substrate-ISIN>.csv` — implement per Step 4. ROLEXRINGS's three events are all in-window → product 1000; an out-of-window event like PATANJALI/WAAREEINDO would be excluded BEFORE the magnitude test.)

> **NPST is deliberately NOT in this parametrize (F4).** NPST is one bonus counted twice → applied 9 → `applied_effective ≈ 9`, gap ≈ 2.86 → `9/2.86 ≈ 3.15`, which is **below** the `>5×` magnitude/log-5 red threshold. The magnitude detector would NOT catch it, and that is correct — NPST is caught by the **count-conflict signal** (two/three sources claiming the *same* event on near-identical dates+ratios, surfaced in the evidence-bucketing / reconcile dedup of Task 4), not by the factor-vs-gap magnitude ratio. Do NOT add NPST to this parametrize; its resolution is asserted in Task 9 Step 1 (forward-clean case) after the rebuild collapses the double-count to applied 3.

- [ ] **Step 3: Run it — expect FAIL** on the import (`pipeline.corp_action_audit` doesn't exist yet) or, once the helper exists, expect it to PASS on the *current* buggy data (the test asserts the bug is present). Run: `PYTHONPATH=. pytest tests/pipeline/test_corp_action_overadjust.py -v`. Expected at this step: import error (helper missing).
- [ ] **Step 4: Create the audit helper** `pipeline/corp_action_audit.py`. It resolves the candidate event set via the **`07.actions_for` symbol∪substrate-ISIN union** (`by_isin[isin] ∪ by_symbol[symbol]`, dedup by `(ex_date, ratio_factor)`), reads the trading window `[first_trade, last_trade]` from `data/prices/<substrate-ISIN>.csv`, and **applies the date-window guard** before computing any factor (R3). It exposes:
  - `_in_window_actions(isin, symbol)` — **(all ISIN-matched events) ∪ (symbol-added events whose `ex_date ∈ [first_trade, last_trade]`)** (small few-trading-day pre-`first_trade` slack on the symbol-added subset). ISIN-matched events are NEVER window-dropped (D1-F1); only out-of-window **symbol-added** events are excluded here (this is the same scoped filter Task 4 wires into `07`'s call site).
  - `applied_cumulative_factor(isin, symbol)` — product of the **admitted** `ratio_factor`s (all ISIN-matched + in-window symbol-added), matching the `07` `load_prices`/`adj_factor_after` application path; can be < 1 for reverse splits.
  - `applied_effective(isin, symbol)` — the price-MULTIPLIER equivalent of the WHOLE product. **For a multi-event product `P = applied_cumulative_factor(...)`, `applied_effective = max(P, 1/P)`** (F-2 — the single clean rule; do NOT apply the per-event `1/rf`-vs-`rf` sign rule to a product). For ROLEXRINGS `P=1000 → 1000`; KAUSHALYA `P=0.01 → 100`. This is what is comparable to `observed_max_gap` (always ≥ 1).
  - `observed_max_gap(isin, symbol)` — the largest near-ex-date raw-close gap over the admitted ex-dates. **Reuse `pipeline.price_gap.detect_gap` (Task 2) for the window math** — do NOT re-implement a second gap scanner (R4; the two must never drift). The gap window is **series-index-adjacent, NOT calendar** (F-3 — see Task 2). (Task 2 is authored after Task 1 in plan order, so per the BUILD-ORDER note above, build Task 2 first or stub the import.)
  Keep it pure/read-only.
- [ ] **Step 5: Run — expect PASS** (the bug IS present in current data): the detector confirms ROLEXRINGS is over-adjusted. This is our red baseline that the fix must flip.
- [ ] **Step 6: Commit.** `git add tests/pipeline/test_corp_action_overadjust.py pipeline/corp_action_audit.py && git commit -m "test(d1): over-adjustment detector (price-gap mismatch), red baseline"`

---

### Task 2: Price-gap measurement helper (shared, the referee)

**Files:**
- Create: `pipeline/price_gap.py`
- Test: `tests/pipeline/test_price_gap.py`

- [ ] **Step 1: Write the failing test** with a synthetic price series (10:1 split = price drops 10x on ex-date):

```python
# tests/pipeline/test_price_gap.py
from pipeline.price_gap import detect_gap

def test_detects_ten_to_one_split():
    series = [("2025-10-15", 1000.0), ("2025-10-16", 1000.0),
              ("2025-10-17", 100.0), ("2025-10-18", 101.0)]  # 10x drop on ex
    g = detect_gap(series, "2025-10-17", window=3)
    assert g is not None
    assert abs(g.ratio - 10.0) < 0.5
    assert g.direction == "down"   # forward split: price drops

def test_no_gap_when_flat():
    series = [("2025-10-15", 100.0), ("2025-10-16", 101.0), ("2025-10-17", 99.0)]
    assert detect_gap(series, "2025-10-17", window=3) is None

def test_gap_spans_a_data_hole():  # F-3 — series-INDEX-adjacent, NOT calendar
    # a multi-week (or multi-month) hole around the ex-date: the gap pair is the two
    # index-adjacent closes straddling the hole, NOT rejected for date-distance.
    # Mirrors ROLEXRINGS (15-month hole, ~20x) / KAUSHALYA (~3.5-week suspension, ~100x).
    series = [("2024-07-05", 2516.95), ("2025-10-17", 126.10), ("2025-10-18", 127.0)]
    g = detect_gap(series, "2025-10-17", window=3)
    assert g is not None and g.ratio > 15.0 and g.direction == "down"
```

- [ ] **Step 2: Run — FAIL** (module missing). `PYTHONPATH=. pytest tests/pipeline/test_price_gap.py -v`
- [ ] **Step 3: Implement** `detect_gap(series, ex_date, window)` returning a small dataclass `Gap(ratio, direction, gap_date)` — the max adjacent-day close ratio within `window` **series-index positions** of `ex_date`. **The window is series-INDEX-adjacent, NOT calendar-day-based (F-3 — load-bearing):** find the first series index at/after `ex_date`, scan `±window` POSITIONS in the sorted close series. A corp-action that coincides with a data hole still produces an index-adjacent gap pair — this is INTENTIONAL and is exactly how ROLEXRINGS's ~20× (a 15-MONTH series hole, `2024-07-05 2516.95 → 2025-10-17 126.10`) and KAUSHALYA's ~100× (a ~3.5-week suspension, `2024-01-11 9.85 → 2024-02-06 988.3`) are measured. **Do NOT add a calendar-proximity filter (e.g. "reject pairs > N calendar days apart")** — it would return `None` for both and silently break the whole detector. This MUST match the `observed_max_gap` helper in Task 1 so the two never disagree across a weekend/holiday/hole. `direction="down"` if the later close is smaller (forward split), `"up"` if larger (reverse split/consolidation); `None` if no ratio exceeds a noise threshold (e.g. 1.5×) or coverage is missing. No `Date.now()`.
- [ ] **Step 4: Run — PASS.**
- [ ] **Step 5: Commit.** `git commit -am "feat(d1): price-gap detector (the senior referee)"`

---

### Task 3: Override table for price-ambiguous cases (from Wave-1 evidence)

**Files:**
- Create: `data/reference/corp_action_overrides.csv` (seed from Wave-1)
- Create: `pipeline/corp_action_overrides.py` (loader)
- Test: `tests/pipeline/test_corp_action_overrides.py`

- [ ] **Step 1: Read** `data/master/review/corp_action_external_evidence.csv` to extract the price-ambiguous resolutions (verdicts `resolve-apply-once` / `resolve-correct-ratio` / `resolve-reverse-direction` where the price gap could NOT auto-settle it — e.g. ROLEXRINGS coverage hole). Seed `corp_action_overrides.csv` with columns `isin,symbol,ex_date,canonical_factor,direction,reason,evidence_url`. The `reason` MUST record WHY the override is needed (e.g. `coverage_hole`, `price-vs-web conflict: gap≈20× but NCLT 10×`, etc.) — this is load-bearing for the contradiction guard (Step 4b). **F-4 (seeding robustness):** (a) the evidence CSV has NO `canonical_factor`/`direction`/`ex_date` column and has **comma-bleed/quoting drift** in `web_source_urls`/`notes` — **parse it with quoted `csv.DictReader`, NEVER a bare `cut`/split**, and map `recommended_verdict` + the prose summaries to a numeric `canonical_factor` by hand; (b) **pin the ROLEXRINGS seed row literally** so the tests are unblocked deterministically: `INE645S01024,ROLEXRINGS,2025-10-17,10.0,down,coverage_hole,<evidence_url>`; (c) scope the seed to ONLY the rows the tests / Task 9 need (ROLEXRINGS + any STAYS-FLAGGED that gets an override), deferring full Wave-1 ingestion to the TODO.
- [ ] **Step 2: Write failing test:** loader returns the ROLEXRINGS override (factor 10, applied once) and returns `None` for an ISIN with no override.

```python
from pipeline.corp_action_overrides import override_for
def test_rolexrings_override():
    # R1: key on the SUBSTRATE ISIN (the key the evidence CSV + substrate use); there
    # is no INE645S01016 price file/substrate row.
    o = override_for("INE645S01024")
    assert o and abs(o.canonical_factor - 10.0) < 0.01 and o.apply_once is True
def test_no_override_returns_none():
    assert override_for("INE000000000") is None
```

- [ ] **Step 3: Run — FAIL.**  **Step 4: Implement** the CSV loader (`override_for(isin)`; key the seed row on substrate `INE645S01024`, or have the loader resolve by symbol).  **Step 5: Run — PASS.**
- [ ] **Step 6 (F3 — contradiction guard, closes the §6 back-door): Write a failing test then implement a loader assertion.** An override is honored ONLY if **(a)** the stock's price gap is absent / ambiguous / a coverage-hole (the gap can't settle it), **OR (b)** the override's `reason` explicitly records a price-vs-web conflict with cited evidence (`evidence_url` non-empty). If an override exists for a stock whose price gap is **clean and cleanly contradicts** the override factor **with no such reason**, the loader must **hard-fail** (raise) — an override may never silently overrule a clean, unambiguous price gap. Test cases: (i) ROLEXRINGS (substrate `INE645S01024`, coverage-hole reason cited) → honored; (ii) a synthetic override that contradicts a clean ~10× gap with an empty/no-conflict reason → loader raises; (iii) the same contradiction but with a cited `price-vs-web` reason → honored. **Run — PASS** after implementing the guard in the loader (it needs the price gap, so the guard takes a `detect_gap` callback or the measured gap as input — wire it where the reconcile calls the loader, Task 4).
- [ ] **Step 7: Commit.** `git commit -am "feat(d1): corp-action override table (price-ambiguous cases, Wave-1 sourced)"`

---

### Task 4: Rebuild reconciliation around price-validation (`03k`/`03l`)

**Files:**
- Modify: `pipeline/03k_reconcile_corporate_actions.py` — **the Yahoo dedup that must be REPLACED is the 7-day same-ratio collapse in `load_yahoo_data` at lines 75-91** (this is the bug locus). The reconcile *matching* loop at 145-195 (the ±10-day NSE match + 5% ratio tolerance) is also touched, but the blind dedup lives at 75-91.
- Modify: `pipeline/03l_merge_corporate_actions.py` — the union/append is the `pd.concat` at **line 58** (44-53 is the Yahoo→NSE row-shaping loop).
- Modify: `pipeline/07_returns_summary.py` — **wire the date-window gate (symbol-added subset only, D1-F1) into the live apply path AT THE CALL SITE (D1-F2)** so EVERY downstream consumer (returns, MFE/MAE, listing metrics, issue/terminal adjustment) is protected and stays IN SYNC, not just the audited rows. `actions_for` = **114-127** (the union); the factor is applied in `load_prices` = **206-235** (series), `adj_factor_after` for issue→adj_issue = **239-246 / called at 438**, and `adj_factor_after` for the delisting terminal = **called at 270**. **The two call sites where actions+prices meet (~599-600 test-mode, ~630-631 main) build the windowed `actions` list ONCE and pass the SAME list to BOTH `load_prices` AND `compute()`** — gating only inside `load_prices` would desync issue/terminal from the series (D1-F2). (See Step 6 below.)
- **Modify (F-7 — define + plumb the flag carrier column(s)):** `pipeline/07_returns_summary.py` `columns()` (**ends ~530-549**, currently terminates at `listing_metrics_status`, `price_source`) — **add the new flag columns `corp_action_out_of_window`, `corp_action_unresolved`, `corp_action_envelope_violation`, and `corp_action_no_price_gap` (R9 — the per-action price-validation phantom-drop flag)** so they exist in `returns_summary.csv`. They flow automatically into `09` via `ret_keys` (`09:41` builds `ret_keys` from the returns-summary keys → `ret_cols` at 44 → `out_cols` at 45 → `ipo_analysis.csv`), so **no extra `09` column wiring is needed beyond confirming they appear in `out_cols`**. Without this column the flags Tasks 4/5/5b set are inert and Task 6 has nothing to read.
- Test: `tests/pipeline/test_corp_action_reconcile.py` + `tests/pipeline/test_actions_for_window.py` (the `07` call-site window gate) + a regression guard for D1-F1 (ATLANTAA/TARIL ISIN-matched pre-coverage splits stay applied)

> **DAG note (N3, + R9):** `03k`/`03l` are standalone scripts — they are NOT wired into `run_all.py` or `project_map.py`'s DAG (and not into `corp_actions_merged` regeneration in any runnable chain). So **rebuilding `03k`/`03l` does NOT by itself flow into the substrate** — the substrate is patched directly in Task 8, whose patch tool replicates the `07` compute path. (Wiring `03k`/`03l` into a runnable DAG is deferred to TODO-D1c/d; until live-capture they stay orphaned. See Task 10.) **The call-site date-window gate AND the per-action price-validation added to `07` (Step 6 + Step 6b) are the parts that REACH the substrate**, because Task 8's patch reuses `07`'s `compute()`/`load_prices` — so the `07` call site is what actually drops the out-of-window **symbol-added** attachments AND the in-window phantom / same-event double-counts from the patched rows. **`03k`/`03l` are the OFFLINE source-of-truth, but the SUBSTRATE enforcement of "price disposes" is the `07`-path per-action price-validation (Step 6b), NOT the orphaned reconcile** — this is the R8 fix: without it, in-window phantoms / same-event double-counts (CANTABIL 25→5, GNA 4→2, RPEL/VISHWARAJ/GICL/PAVNAIND/KNRCON/ASIANTILES) would survive the window gate (in-window), the envelope tripwire (blind to uniform over-counts), and the override table (named seeds only). (Real OOW union count = **86** total; of those only the symbol-added subset is droppable — the ISIN-matched OOW actions ALWAYS apply, D1-F1.)

- [ ] **Step 1: Read** both files fully — specifically `03k` `load_yahoo_data` (75-91, the dedup to replace) + the `reconcile` matching loop (145-195), and `03l`'s `pd.concat` (58) — to learn the current event record shape and how `corp_actions_yahoo_only.csv` is appended.
- [ ] **Step 2: Write failing tests** with synthetic multi-source events + a synthetic price series:
  - three identical 10:1 events (Yahoo×2 + NSE) on dates 28 days apart, price gaps once by 10× → reconcile collapses to **one** event, factor 10.
  - a `ratio_factor=0.01` Yahoo "reverse" event whose price gaps UP ~100× → kept as a real consolidation (direction from gap sign).
  - a phantom event (claimed, price never moved) → dropped (not applied).
  - **(R9) IN-WINDOW same-event double-count collapses to ONE (CANTABIL/GNA class):** two same-ratio legs (e.g. a 5:1 reported by yfinance and NSE ~13 days apart), BOTH in-window, but only ONE supporting price gap (~5×) → the validation collapses to a SINGLE application (factor 5, not 25). Use a synthetic series gapping ~5× once. (This is the leg of remedy (a) that the window gate alone does NOT catch — both legs are in-window.)
  - **(R9) IN-WINDOW phantom with PRICE PRESENT → DROP + flag:** a same-ratio leg whose ex-date has surrounding closes that did NOT jump (gap ≈ 1.0, e.g. CANTABIL's yfinance 2023-10-20 leg) → DROPPED + `corp_action_no_price_gap`, never multiplied. Distinct from the next case.
  - **(R9) NO gap because PRICE MISSING (coverage hole) → NOT dropped, deferred to override:** an admitted action whose ex-date sits in a coverage hole (no closes to compare) → `detect_gap` returns None for MISSING data, NOT phantom → consult `override_for`; with a confident override (ROLEXRINGS-style) the canonical factor is applied, NOT dropped. **Assert ROLEXRINGS is NOT dropped by the phantom path** (the coverage-hole-vs-phantom distinction — the load-bearing safety of remedy (a)).
  - **(D1-N1) compound same-day, DIFFERENT-TYPE legs are BOTH retained (NOT collapsed to one):** ISHAN-style same-day (e.g. 2024-01-25) 10:1 split + 2:1 bonus, product **30**, price gaps **28.56×** — both legs legitimate; the "collapse duplicates that map to the same gap" step (Step 4) must KEEP the combined factor (30), not drop a leg to ~one 30× event. (They survive `(ex_date, ratio_factor)` dedup because the ratios differ — 10.0 vs 3.0 — but the new gap-matching reconcile must not naively collapse "one event per gap." `applied_effective` for the combined product = `max(P, 1/P)` = 30.) Without this test the reconcile could regress the 18 compound `ratio-conflict` evidence cases.
- [ ] **Step 3: Run — FAIL.**
- [ ] **Step 4: Implement** the new reconcile: **group candidate events per SUBSTRATE ISIN over the symbol∪ISIN union, then apply the trading-date window gate to the SYMBOL-ADDED subset ONLY (R2 + R3 + D1-F1)** — ISIN-matched events (`ex` ∈ `by_isin[substrate-ISIN]`) are the stock's own events and are ALWAYS kept; only symbol-added events (in `by_symbol` but NOT in `by_isin[substrate-ISIN]`) must pass `ex_date ∈ [first_trade, last_trade]` and out-of-window symbol-added actions are dropped+flagged `corp_action_out_of_window` here, never reconciled into a leak (replace any "group/filter the whole union by the window" wording — that would drop the 26 legit ISIN-matched pre-coverage splits, D1-F1). For each remaining (admitted) event, call `detect_gap` (Task 2 — **series-index-adjacent window**); accept+apply-once when a gap matches the proposed ratio (**direction-normalized** — compare the price-multiplier equivalent to the gap, so reverse splits with `ratio_factor<1` match a gap that goes UP); set direction from gap sign (fixes D-2); collapse duplicates that map to the same gap **BUT keep distinct same-day legs whose PRODUCT matches the gap (D1-N1 — do not drop a leg of a compound split+bonus)**; consult `override_for` (Task 3) for price-ambiguous cases **passing in the measured gap so the override contradiction guard (Task 3 Step 6) can fire**; emit events the price doesn't support to a **flag** channel (not applied). Drop the blind `(ex_date, ratio_factor)` / 7-day same-ratio dedup (`03k:75-91`). **(R9) This per-action price-validation is the SAME logic Step 6b wires into the `07` call site — the reconcile is the offline source-of-truth; Step 6b is the substrate-reaching enforcement. Keep them factored as one shared helper so they cannot drift (the four-way outcome: gap-supports → apply; same-ratio legs → one gap → collapse-to-one; price-present-no-gap → phantom-DROP + flag `corp_action_no_price_gap`; data-missing coverage-hole → defer to `override_for`).**
- [ ] **Step 5: Run — PASS** (reconcile tests).  **Step 6 (R3 + D1-F1 + D1-F2 — gate `07` at the CALL SITE so ALL consumers stay in sync): Write a failing test then implement the date-window gate at the call site.** Add `tests/pipeline/test_actions_for_window.py` with THREE assertions: (i) an out-of-window **symbol-added** event (in `by_symbol` only, `ex_date` before `first_trade` / after `last_trade`) is DROPPED and emits `corp_action_out_of_window`; (ii) an in-window event is kept and a small pre-`first_trade` slack keeps a listing-day split; **(iii) D1-F1 regression guard — an out-of-window event that is ISIN-matched (in `by_isin[substrate-ISIN]`) is ALWAYS KEPT, never dropped** (ATLANTAA-style pre-coverage split). Then implement at **BOTH call sites (~599-600 test-mode, ~630-631 main, D1-F2)**: after the prices are loaded/sorted, read the `[first_trade, last_trade]` window, build the windowed `actions` list ONCE = `(all ISIN-matched events) ∪ (symbol-added events with ex_date in window)`, drop+flag the out-of-window symbol-added ones (`corp_action_out_of_window` → carrier column from F-7 → Task 6's unresolved set), and **pass the SAME windowed list to BOTH `load_prices` AND `compute()`** so the series, `adj_factor_after`(issue→adj_issue, 438) and `adj_factor_after`(terminal, 270) all use one factor (no desync, D1-F2). **Do NOT filter only inside `load_prices`.** This implements join-strategy option (d), scoped to symbol-added per D1-F1, at the point actions+prices meet — making returns/MFE/MAE/listing metrics/issue/terminal all window-safe and consistent. **Run — PASS.**
- [ ] **Step 6b (R9 — remedy (a): PER-ACTION PRICE-VALIDATION at the `07` call site, the substrate-reaching enforcement of "price disposes"): Write a failing test then implement.** This is the R8 fix — without it, in-window phantoms / same-event double-counts survive the window gate (Step 6), the envelope tripwire (blind to uniform over-counts), and the override table (named seeds only). Add to `tests/pipeline/test_actions_for_window.py` (or a sibling `tests/pipeline/test_corp_action_price_validation.py`) FOUR assertions exercising the `07` call-site path against synthetic price series: **(i) gap-supports-ratio → APPLY** (a single 5:1 with a ~5× gap stays a 5× adjustment); **(ii) same-ratio legs mapping to ONE gap → COLLAPSE-TO-ONE** (two in-window 5:1 legs, one ~5× gap → applied factor 5, NOT 25 — the CANTABIL fix; and two in-window 2:1 legs, one ~2× gap → factor 2, NOT 4 — the GNA fix); **(iii) price-present-no-gap → PHANTOM-DROP + flag** (an in-window leg whose ex-date neighbours are flat → DROPPED, `corp_action_no_price_gap` set, factor excludes it); **(iv) data-missing coverage-hole → DEFER to override, NOT drop** (an admitted action in a coverage hole with a confident override → applied via `override_for`, NOT phantom-dropped — ROLEXRINGS-style). Then implement at **BOTH call sites (~599-600 test-mode, ~630-631 main)**, immediately AFTER the windowed `actions` list is built (Step 6) and BEFORE it is passed to `load_prices`/`compute()`: for each admitted action call `detect_gap` against the loaded/sorted series and apply the four-way outcome (reusing the SHARED reconcile helper from Step 4 so the two can't drift). Pass the resulting **validated** `actions` list (collapsed + phantom-dropped) to BOTH `load_prices` AND `compute()`, so the series, issue (438) and terminal (270) all use the validated factor. **Crucially distinguish "no gap because phantom" (closes present around ex-date, no jump → DROP) from "no gap because data missing" (coverage hole, no closes → defer to override / flag-unresolved) — never wrongly drop a coverage-hole legitimate case.** Phantom-dropped actions route to the unresolved set (Task 6) via `corp_action_no_price_gap`. **Run — PASS** (CANTABIL collapses 25→5, GNA 4→2; ROLEXRINGS NOT dropped).
- [ ] **Step 7: Commit.** `git commit -am "feat(d1): price-validated corp-action reconciliation + call-site date-window join gate (symbol-added only) + per-action price-validation (R8 remedy a: phantom-drop/same-event-collapse reaches the substrate) in 07 (replaces blind dedup; fixes D-2)"`

---

### Task 5: Continuity guard + close the `09` cross-source hole (flag-only)

**Files:**
- Modify: `pipeline/07_returns_summary.py` (post-adjustment) and/or `pipeline/09_assemble.py` — the cross-source listing-price check is **skipped for corp-action stocks** via the `and not has_action` clause at **line 89** (the `has_action` set is built at 86-88; line 70 is the unrelated "INVARIANT CLAMP REMOVED" comment). The fix removes that skip clause so the check runs for corp-action stocks too.
- Modify: a review register under `data/master/review/` (e.g. `corp_action_continuity_flags.csv`)
- Test: `tests/pipeline/test_continuity_guard.py`

> **This continuity guard is DISTINCT from the envelope tripwire (Task 5b).** Continuity = single-day *price* jumps in the adjusted series (a leftover unexplained discontinuity). The envelope tripwire = `trough ≤ endpoint ≤ peak` consistency of the MFE/MAE outcome columns. Both are flag-only; neither clamps.

- [ ] **Step 1: Read** `09_assemble.py` lines 82-94 (the cross-source check; the `and not has_action` **skip is at line 89**, set built 86-88) + the `07` adjusted-price output. Note it is **skipped for corp-action stocks**, NOT globally disabled (N2).
- [ ] **Step 2: Write failing test:** an adjusted series with a leftover unexplained ~5× single-day jump → guard returns one flag row; a clean series → zero flags. Assert the guard **never mutates** prices (flag-only).
- [ ] **Step 3: Run — FAIL.  Step 4: Implement** `check_continuity(adjusted_series) -> [flags]` and wire it into `07`/`09`; re-enable the `09` cross-source listing-price check for stocks WITH corp actions (**remove the `and not has_action` skip clause at line 89**), routing disagreements to flags.
- [ ] **Step 5: Run — PASS.  Step 6: Commit.** `git commit -am "feat(d1): flag-only continuity guard + re-enable 09 cross-source check for corp-action stocks"`

---

### Task 5b: Loud ENVELOPE TRIPWIRE — permanent `trough ≤ endpoint ≤ peak` invariant (T-4 decision (c) part 2)

> This implements the **owner-locked T-4 decision (c) part 2** (`docs/research/alignment_audit_2026-06-16.md:320-326`): the silent MFE/MAE clamp was REMOVED; it must be replaced with a **permanent loud tripwire** so a future bad factor (the next 1000× error) surfaces as a FLAG instead of being silently snapped. **This is DISTINCT from the continuity guard (Task 5, single-day price jumps)** — this asserts the *outcome envelope* `trough ≤ endpoint ≤ peak` per row/horizon. **FLAG/RAISE, NEVER CLAMP.**

**Files:**
- Modify: `pipeline/07_returns_summary.py` (after the MFE/MAE computation in `_mfe_mae_block`, ~lines 306-360) OR a post-assemble check in `pipeline/09_assemble.py` — pick the locus that sees the final per-row/per-horizon MFE/MAE/endpoint values.
- Modify/create: a review register under `data/master/review/` (e.g. `corp_action_envelope_flags.csv`)
- Test: `tests/pipeline/test_envelope_tripwire.py`

- [ ] **Step 1: Read** `07`'s `_mfe_mae_block` (306-360) to learn the per-horizon column names (`mfe_*`/`mae_*`/`return_from_issue_*`/`current_return_from_issue`, both `_lst` entries) and where the final values are assembled.
- [ ] **Step 2: Write failing tests** (synthetic, flag-only — assert the tripwire flags violations, passes clean rows, and NEVER mutates):
  - a synthetic row whose `mae_h > return_from_issue_h` (or `return_from_issue_h > mfe_h`) for some horizon → tripwire emits exactly one flag row routed to the review register.
  - a clean row where `mae_h ≤ return_from_issue_h ≤ mfe_h` for every horizon (both entries) → zero flags.
  - assert the tripwire **does not change any MFE/MAE/endpoint value** (read the row before and after — byte-identical; it only flags).
- [ ] **Step 3: Run — FAIL** (tripwire not implemented).
- [ ] **Step 4: Implement** `check_envelope(row_or_block) -> [flags]`: for each horizon and BOTH entries (issue-anchored and listing-anchored), assert `trough ≤ endpoint ≤ peak`; on violation append a flag row (isin, horizon, entry, the three values) to the review register and set the **`corp_action_envelope_violation`** flag on the row (the F-7 carrier column added to `07.columns()` in Task 4 — it flows to `ipo_analysis.csv` via `09`'s `ret_keys` so Task 6 can read it). **Do NOT clamp/snap** — the value is left as-is and the row is flagged for the unresolved set (Task 6 / Task 9 picks up envelope residuals). Wire it as a permanent step (runs every build), not a one-off.
- [ ] **Step 5: Run — PASS.  Step 6: Commit.** `git commit -am "feat(d1/T-4c): permanent loud envelope tripwire (flag trough<=endpoint<=peak violations, never clamp)"`

---

### Task 6: Unresolved-row handling — flag + null outcome (+ outcome_class)

**Files:**
- Modify: `pipeline/09_assemble.py` (where `data_quality_tier`/flags are set + outcomes joined)
- Test: `tests/data/test_unresolved_corp_action_rows.py`

- [ ] **Step 1: Read** how `data_quality_tier` and outcome columns are assigned in `09`. Also read `07.columns()` (post-Task-4) to confirm the carrier columns `corp_action_out_of_window` / `corp_action_unresolved` / `corp_action_envelope_violation` / **`corp_action_no_price_gap` (R9)** exist and reach `ipo_analysis.csv` via `ret_keys` (F-7) — without them this whole task is inert.
- [ ] **Step 2: Write failing test:** for an ISIN in the unresolved set (the 6 Wave-1 STAYS-FLAGGED: CMMIPL, INDUSFILA, BANSAL, COOLCAPS, SILVERTUC, VAISHALI), assert the row EXISTS, `corp_action_unresolved` flag is set, `data_quality_tier` is the low tier, and the unreliable outcome columns (returns/alpha/MFE/MAE) **and `outcome_class`** are NULL — while a normal stock keeps its values.
- [ ] **Step 3: Run — FAIL.  Step 4: Implement:** build the **unresolved set dynamically** = the 6 Wave-1 STAYS-FLAGGED **+ any ISIN the Task-4 rebuild routed to the flag channel (price-unsupported, no override) + any ISIN with a `corp_action_out_of_window`-dropped action (the date-window gate, SYMBOL-ADDED subset only per D1-F1, Task 4 Step 6) + any ISIN with a `corp_action_no_price_gap`-dropped PHANTOM action (the per-action price-validation, R9 remedy (a), Task 4 Step 6b) + any ISIN the Task-5b envelope tripwire flagged (`corp_action_envelope_violation`) + any T-2 envelope residual (Task 9)**. For EVERY ISIN in that set: set the `corp_action_unresolved` flag + low `data_quality_tier` + null `outcome_class` + null the unreliable outcome columns. **NULL-SET (F-8 — enumerate explicitly; define as a named constant the test imports so test + impl can't drift):** all `return_from_issue_*`, `return_from_listing_*`, `alpha_*`, `alpha_sc_*`, `mfe_*`, `mae_*`, `mfe_lst_*`, `mae_lst_*`, `days_to_mfe_*`, `days_to_mae_*`, `days_to_breakeven_*`, `current_return_from_issue`, `max_gain_pct`, `max_drawdown_pct`, `max_drawdown_duration_days`, `all_time_high`, `all_time_low`, `listing_gain_open`, `listing_gain_close`, `volatility_annual`, and `outcome_class`. **KEEP (never null):** identity columns, `issue_price`/`issue_price_adj`, `listing_date`, `n_days_history`, `liquidity`/turnover identity, all flag columns, `delisted`/`delist_reason`. **Do NOT drop the row**, and **never write a guessed value** (per the Confidence Invariant). Only confidently-resolved stocks (price-validated or confident override) carry final values. *(Note: a stock whose ONLY corp action was an out-of-window SYMBOL-ADDED action that got dropped now has NO admitted symbol-added event — its raw series is unadjusted and may be correct; route it to flag+null per the invariant unless its gap is clean, since the drop signals a symbol-reuse/wrong-era ambiguity. ISIN-matched actions are never dropped, so they do NOT land here via the gate, D1-F1.)* *(R9 note: a same-event double-count that the price-validation COLLAPSED to one (CANTABIL/GNA) is **confidently resolved** — the price gap validated the single real factor — so it carries final values and does NOT land in the unresolved set. Only a PHANTOM leg dropped because price was present-but-flat (`corp_action_no_price_gap`) routes the row to flag+null, per the confidence invariant.)*
- [ ] **Step 5: Run — PASS.  Step 6: Commit.** `git commit -am "feat(d1): flag+null genuinely-unresolved corp-action rows (keep row, null bad outcomes+label)"`

---

### Task 7: Explicit delisting −100% (T-4), decoupled from the clamp

**Files:**
- Modify: `pipeline/07_returns_summary.py` (synthetic/terminal handling)
- Test: `tests/pipeline/test_compute_synthetic.py` (the existing `test_compulsory_delisting_forces_minus_100`)

- [ ] **Step 1: Read** the existing failing test + the synthetic/delisting logic in `07` (`data/master/delisting.csv` usage AND the **`_mfe_mae_block` at lines 306-360**).
- [ ] **Step 2: Confirm the test fails** today AND pinpoint WHERE: `PYTHONPATH=. pytest tests/pipeline/test_compute_synthetic.py::test_compulsory_delisting_forces_minus_100 -v` → FAIL. **The failing assertion is `mae_1y == -1.0` at line 110** (it gets **+0.15** — the MFE/MAE *trough*). The horizon-terminal return (`return_from_issue_1y == -1.0` at **line 106**) **ALREADY PASSES** — do NOT touch the terminal/`price_at_horizon` logic, it is already correct.
- [ ] **Step 3: Implement explicit enforcement IN THE MFE/MAE BLOCK** (`_mfe_mae_block`, ~306-360), NOT the terminal logic: for a compulsory-delisting/liquidation ISIN, force **MAE = −1.0** (the trough must reach the wipeout) and force any horizon **whose window spans the delisting date** to endpoint AND MAE = −1.0 (horizon-aware: earlier horizons that close before the delist date keep the real path). This is independent of the removed clamp and distinct from Task 6's null (a genuine wipeout is −100%, NOT null).
- [ ] **Step 4: Run — PASS** (`test_compulsory_delisting_forces_minus_100`, specifically line 110 `mae_1y` now −1.0; line 106 stays green). Also re-run `tests/layer3/test_reach.py` + `tests/data/test_substrate_integrity.py::test_trough_le_endpoint_le_peak` (T-2) — expect improvement once Task 8 rebuilds data; here just confirm no regression in synthetic tests.
- [ ] **Step 5: Commit.** `git commit -am "fix(d1/T-4): explicit horizon-aware delisting -100% (decoupled from removed clamp)"`

---

### Task 8: Phase-3 targeted patch — inventory remediations, recompute affected stocks, diff

**Files:**
- Create: `tools/d1/inventory_manual_remediations.py` (one-off audit) + `docs/research/d1_remediation_inventory.md`
- Create: `tools/d1/apply_targeted_patch.py`
- Modify: `data/master/ipo_analysis.csv` (+ `returns_summary.csv`, `universe.csv` as needed) — affected stocks only
- Test: `tests/data/test_d1_targeted_patch.py`

- [ ] **Step 1 (GATE): Inventory the post-pipeline manual remediations.** Read `docs/data_review.md`, `WORKFLOWS.md` (frozen-substrate note), git history of `data/master/`, and DRHP staging. **MUST enumerate `data/reference/manual_overrides.csv`** (the `isin,column,value,reason,date` overlay applied in `09_assemble.py:98-112`) — it is a known remediation channel a recompute must preserve. Write `docs/research/d1_remediation_inventory.md` listing every manual fix that a recompute must NOT clobber. **Do not proceed to Step 3 until this exists and is reviewed.**
- [ ] **Step 2: Write failing test:** after patch, ROLEXRINGS `current_return_from_issue` is ~+50% (not +15,272%) and `outcome_class != multibagger`; NPST sane; KAUSHALYA's real ~×100 reverse split is unchanged (direction-normalized, already correct); **and D1-F1 regression guard — ATLANTAA (`INE285H01022`) stays a winner (adj_issue 30.0, `current_return_from_issue ≈ +36.3%`, `outcome_class` unchanged) and TARIL (`INE763I01026`) keeps its multibagger label** (their ISIN-matched pre-coverage 5:1 / split is NOT dropped). **(R9 — remedy (a) substrate proof, the in-window same-event double-count fix reaches the patched substrate):** after patch, **CANTABIL (`INE068L01024`)** collapses 25→5 → `current_return_from_issue ≈ +614%` (~7.14×), `outcome_class != multibagger`; **GNA (`INE934S01014`)** collapses 4→2 → `current_return_from_issue ≈ +170%` (~2.70×), `outcome_class != multibagger`. These are NON-seeded, in-window, envelope-consistent — they prove "price disposes" now executes on `07.compute()` (the substrate path), not just the orphaned `03k`/`03l`. (ROLEXRINGS, a coverage-hole case, is NOT phantom-dropped — it resolves via its override seed; assert it stays resolved, not nulled.) **Define the corp-action stock set (R2 + R3 + D1-F1):** substrate ISINs whose **symbol OR ISIN** appears in `corp_actions_merged` (mirror `09:86-89`'s `has_action`, which already unions `action_isins` and `action_symbols`) **AND that have ≥1 ADMITTED event** = any ISIN-matched event (always admitted) OR an in-window symbol-added event after the date-window gate. **FULL ANTI-JOIN control (F6 + D1-M1):** assert that **EVERY ISIN NOT in this corp-action set is identical to the snapshot — compared over the PRE-EXISTING column set ONLY** (D1-M1 — the new `corp_action_*` columns re-serialize every row, so a raw byte compare would false-fail all 2384 rows; restrict each non-corp-action row to the snapshot's column set and additionally assert the new flag columns are empty on those rows). The out-of-window-only **symbol-added** rows correctly fall OUTSIDE the patched set (their dropped action means no adjustment change → they should match the snapshot once the leak is fixed; if such a row WAS previously distorted by the out-of-window symbol-added action, it moves into the patched set via its flag and is flag+nulled by Task 6). ISIN-matched OOW actions never trigger a drop, so their stocks stay correct (D1-F1).
- [ ] **Step 3: Implement `apply_targeted_patch.py`:** recompute price-derived columns ONLY for the corp-action stocks (the set defined in Step 2), **reusing `07`'s `compute()` / `load_prices` (the real adjustment + MFE/MAE path, now with the R3 date-window gate AND the R9 per-action price-validation) — do NOT re-implement the price math** (re-implementation drifts from `07`; and recall from N3 that rebuilding `03k/03l` alone does NOT reach the substrate, so the patch IS the path that applies the corrected reconcile + window gate + per-action price-validation to the substrate — the phantom-drop / same-event-collapse logic (Task 4 Step 6b) is INHERITED automatically because the patch calls `07.compute()`). Write back into the existing substrate, leave all other rows untouched. Apply the `manual_overrides.csv` overlay last so a recompute can't clobber it. **Update `substrate_meta.json` deliberately (R5):** the **row count does NOT change** (the patch nulls outcomes / re-derives values but keeps every row); the fields expected to MOVE are the **derived distributions** — `outcome_class` counts (ROLEXRINGS/NPST **and the R9 in-window double-counts CANTABIL/GNA** leave `multibagger`; **ATLANTAA/TARIL stay put**, D1-F1), the flagged-row tallies (the NEW columns `corp_action_unresolved` / `corp_action_out_of_window` / `corp_action_envelope_violation` / **`corp_action_no_price_gap` (R9 phantom-drop)** are added to every row and their non-empty counts grow), and any `data_quality_tier` distribution. **NOTE the new `corp_action_*` columns (`corp_action_out_of_window` / `corp_action_unresolved` / `corp_action_envelope_violation` / `corp_action_no_price_gap`) are added to the schema (F-7 + R9) → R5 must name them explicitly so the D1-M1 anti-join excludes them from byte-identity (compare over the pre-existing column set).** State these in the diff so the `tests/data/` GOLDEN headline invariants are updated deliberately, not reactively. If `substrate_meta.json` carries an explicit row count, it stays the same.
- [ ] **Step 4: Run — PASS.** Then verify only intended rows changed: **full anti-join diff over the pre-existing column set (D1-M1)** of every non-corp-action ISIN against the snapshot is empty (the gating assertion) AND the new `corp_action_*` columns are empty on every non-corp-action row; `git diff --stat data/master/` as a sanity glance only (it shows file churn, not which rows). Run `PYTHONPATH=. python verify.py` → PASS.
- [ ] **Step 5: Commit.** `git commit -am "fix(d1): targeted patch of corp-action stocks in substrate (snapshot+diff verified)"`

---

### Task 9: Verify the bug is dead + tests flip green

- [ ] **Step 1:** Flip Task 1's detector to assert RESOLVED — **THREE-way**, using the RIGHT criterion per case (the observed gap is only valid for clean stocks, and the comparison is direction-normalized via `applied_effective`):
  - **(a) Forward-clean** (e.g. NPST: after the rebuild collapses the double-count, applied = 3, `applied_effective ≈ 3` vs gap ≈ 2.86; **and the R9 in-window same-event double-counts — CANTABIL: after collapse applied = 5, `applied_effective ≈ 5` vs gap ≈ 5; GNA: after collapse applied = 2 vs gap ≈ 2**) → assert `applied_effective / max_gap ≈ 1` (within tolerance). *(Pre-fix `applied_cumulative_factor` was the SQUARED value — NPST 9 = 3×3, CANTABIL 25 = 5×5, GNA 4 = 2×2; the assertion is only valid AFTER the per-action price-validation collapses each to one (Task 4 Step 6b → inherited by the Task-8 patch) — state this so it isn't confused with the pre-fix value. These are the proof that remedy (a) reaches the substrate: the validated factor now matches the gap.)*
  - **(b) Reverse-clean** (KAUSHALYA: single 1:100 reverse split, `ratio_factor = 0.01`, price gaps UP ~×100) → **direction-normalized**: `applied_effective = 1/0.01 = 100` vs gap ≈ 100 → `applied_effective / max_gap ≈ 1` ✅ (untouched, correct). This is the reverse-split-MUST-PASS guardrail. *(Do NOT use the raw `applied_cumulative_factor` = 0.01 here — that is the F1 bug; use `applied_effective`.)*
  - **(c) Coverage-hole / override** (ROLEXRINGS — its observed gap is unreliable BY DEFINITION, which is *why* it's an override) → do NOT use the ratio; instead assert the resulting `current_return_from_issue` is sane (~+50%) and `outcome_class != multibagger` — the same check as Task 8 Step 2.
  Update + run → PASS.
- [ ] **Step 2:** Run the previously-failing T-2 envelope tests (`tests/data/test_substrate_integrity.py::test_trough_le_endpoint_le_peak`, `tests/layer3/test_reach.py`) → expect cleared (or a short residual list to add to the unresolved set, Task 6). **Also verify the substrate delisting test (N5):** `tests/data/test_substrate_integrity.py::test_compulsory_or_liquidation_delisting_is_minus_100` (**line 103** — asserts `current_return_from_issue == −1.0` for all compulsory/liquidation rows AND `m.sum() >= 30`) → PASS on the patched live data (it gates the T-4 fix on real data, not just the synthetic fixture). T-4 synthetic test → PASS.
- [ ] **Step 3:** Full suite: `PYTHONPATH=. pytest tests -q 2>&1 | tail -3`. Expect the T-2/T-4 cluster green; remaining reds = T-3 (goldens/weights, handled next, separate) + T-5 (thinktank, out of scope). Record the new count.
- [ ] **Step 4:** `PYTHONPATH=. python verify.py` → PASS. **Commit.** `git commit -am "test(d1): detector flipped to resolved; T-2/T-4 green"`

---

### Task 10: Record the follow-up TODOs (durable)

**Files:**
- Modify: `docs/tracker/improvement_backlog.md`

- [ ] **Step 1:** The D-1 follow-up TODOs are **already recorded** in `docs/tracker/improvement_backlog.md` (TODO-D1a…f, added 2026-06-16). CONFIRM they're still accurate after the build and update any that drifted (e.g. the unresolved-set membership once Task 6/9 finalize it). Do NOT duplicate them.
_(PR is opened in Task 12, after review + cleanup.)_

---

### Task 11: Independent final code review (whole diff)

> Per-task review is already handled by `subagent-driven-development`. THIS is the additional **holistic end-to-end review** of the full branch diff (execution-pipeline mandate: build → independent REVIEW → fix → test → cleanup).

**Files:** review only (no edits by the reviewer).

- [ ] **Step 1:** Spawn an independent reviewer (`superpowers:code-reviewer`) on the full branch diff (`git diff main...HEAD`) + the spec. It must verify: (a) behavior matches the spec; (b) the over-adjustment detector uses **direction-normalized factor-vs-gap mismatch** (via `applied_effective`), not raw magnitude (KAUSHALYA's reverse-split ~×100 untouched; NO PATANJALI example anywhere); (c) **the CONFIDENCE INVARIANT held** — no guessed value written for any unsure stock; every unsure stock is flag+null; the override contradiction guard (Task 3 Step 6) is wired; (d) the targeted patch (Task 8) changed **only** corp-action stocks — verified by a **FULL ANTI-JOIN over the pre-existing column set (D1-M1)**: every ISIN not in the corp-action set is identical to the snapshot on the pre-existing columns (not just one control row) and the new `corp_action_*` columns are empty on those rows; `manual_overrides.csv` preserved; (e) genuine wipeouts read −100%, unresolved read null (clean separation); the **envelope tripwire (Task 5b)** is present, permanent, and flag-only (never clamps); (f) **the JOIN KEY is symbol∪substrate-ISIN with the date-window gate scoped to the SYMBOL-ADDED subset ONLY (R2 + R3 + D1-F1)** — ISIN-matched actions ALWAYS apply (the ATLANTAA/TARIL/ADANIPORTS pre-coverage splits stay applied, NOT dropped); the detector keys ROLEXRINGS on substrate `INE645S01024` (not `INE645S01016`), the reconcile groups per substrate ISIN over (all ISIN-matched + in-window symbol-added) events, `07`'s **call-site** gate (NOT inside `load_prices`, D1-F2) passes one windowed `actions` list to BOTH `load_prices` AND `compute()` so issue/terminal stay in sync with the series, the Task-8 patch set admits any ISIN-matched OR in-window symbol-added event, and the `corp_action_out_of_window` flag drops only the out-of-window **symbol-added** (reused-symbol/wrong-era) attachments into the unresolved set; **the F-7 carrier columns (`corp_action_out_of_window` / `corp_action_unresolved` / `corp_action_envelope_violation` / `corp_action_no_price_gap`) are defined in `07.columns()` and reach `ipo_analysis.csv` via `09`'s `ret_keys`** (the flag mechanism is not inert); **(g) (R9 — remedy (a)) PRICE-VALIDATION REACHES THE SUBSTRATE:** the per-action `detect_gap` validation runs at the `07` call site (Step 6b), NOT only in the orphaned `03k`/`03l`, so it is inherited by the Task-8 patch (which calls `07.compute()`); same-event double-counts COLLAPSE to one (CANTABIL 25→5 → ~7.14× not multibagger, GNA 4→2 → ~2.70×); price-present-no-gap phantoms are DROPPED + flagged `corp_action_no_price_gap`; coverage-hole legitimate cases are NOT dropped (ROLEXRINGS resolves via override, not nulled) — confirm the phantom-drop-vs-coverage-hole distinction holds and the envelope-tripwire-blind uniform-over-count class is now caught by this validation (not the tripwire); (h) tests genuinely fail-then-pass; `verify.py` PASS.
- [ ] **Step 2:** Action the findings — fix real issues, note nits. Re-run `PYTHONPATH=. python verify.py` + `PYTHONPATH=. pytest tests -q`.
- [ ] **Step 3: Commit** any fixes. `git commit -am "fix(d1): address independent-review findings"`

---

### Task 12: Cleanup + open PR

> Execution-pipeline "cleanup" stage + CLAUDE.md anti-sprawl ("ONE home per info type"; conclude → durable home, archive/delete the write-up).

**Files:** delete scratch; update audit doc; PR.

- [ ] **Step 1: Delete the pure-scratch experiment scripts** (leftover corp-action probing, referenced by nothing): `get_bse.py`, `get_nse.py`, `nse.py`, `parse_splits.py`, `search.py`, `search_ddg.py`, `splits.html`, `bonus.html`, `test_url.py`. Before deleting each, confirm `grep -rl <name>` shows no references; if one IS needed, move it to `tools/` instead of deleting.
- [ ] **Step 2: KEEP (durable / referenced)** — the spec + plan under `docs/superpowers/`; `data/master/review/corp_action_external_evidence.csv` (feeds the override table + TODO-D1a). Do not delete these.
- [ ] **Step 3: Update `docs/research/alignment_audit_2026-06-16.md`** — POP the now-resolved D-1 / D-2 / T-2 / T-4 items (mark done; detail → git log). **Do NOT delete the audit doc** — it still tracks the non-D-1 open items (O-series, SR-*, C-2, T-5). Full audit-doc retirement is a SEPARATE cleanup for when the whole audit closes.
- [ ] **Step 4: Verify + open PR.** `PYTHONPATH=. python verify.py` → PASS; `git status` → only intended changes. Stage only themed files (exclude triage/data/scratch per repo convention). `git push -u origin fix/d1-corp-action-overcounting` then `gh pr create` (title/body summarizing D-1 + D-2 + T-2 + T-4; note T-3 re-derive + flagged-row resolution are follow-ups TODO-D1a/D1b).

---

## Follow-up (separate PR, after this merges): T-3 re-derive
Re-derive `scorecard_weights.json` + re-bless goldens (`run_weights.py` / `tools/refresh/derive_goldens.py`) on the corrected substrate; re-run mutation/showdown gates. NOT in this plan — it runs once the flagged rows are also resolved (spec §4 #1–#2).

---

## Self-Review (against the spec + the 2026-06-17 independent review + re-review + join-strategy verdict + R5 implementability + R6 final-correctness + R8 adversarial / R9 remedy-(a) fold)
- **Spec coverage:** principle (Tasks 2,4) · Phase 1 rebuild + auto-derive + override + **override contradiction guard** + **date-windowed symbol∪ISIN join (R3/option-d)** + **per-action price-validation at the `07` substrate path (R9 remedy (a), Task 4 Step 6b — phantom-drop / same-event-collapse / coverage-hole-defer)** (Tasks 2,3,4) · strict-mode dropped (Task 4) · Phase 2 continuity guard + 09 hole (Task 5) · **permanent loud envelope tripwire — T-4 decision (c) part 2 (Task 5b)** · flag+null+outcome_class + `corp_action_out_of_window` in the unresolved set (Task 6) · genuine-wipeout −100%/T-4 in the MFE/MAE block (Task 7) · Phase-3 targeted patch + inventory gate (incl. `manual_overrides.csv`) + symbol-OR-ISIN-with-in-window-event patch set + snapshot/full-anti-join diff (Tasks 0,8) · verify three-way + substrate delisting test (Task 9) · TODOs incl. reproducibility-overlay + live-capture watermark + broader checks (Task 10, durable in backlog) · **independent final review (Task 11)** · **cleanup + branch→PR (Task 12)** · TDD detector-first (Task 1). All covered.
- **Canonical join key (R3 / option (d), SCOPED by R6/D1-F1) — symbol∪substrate-ISIN with the date-window gate on the SYMBOL-ADDED subset ONLY, EVERYWHERE:** ISIN-matched actions ALWAYS apply (the stock's own event — keeps the 26 legit pre-coverage splits incl. ATLANTAA/TARIL/ADANIPORTS/TITAGARH/BAJAJCON); only symbol-added (reuse-risk) actions are window-gated. Detector keys ROLEXRINGS on substrate `INE645S01024` + symbol with the admitted-event union (Task 1); reconcile groups per substrate ISIN over (all ISIN-matched + in-window symbol-added) events (Task 4 Step 4); the gate is wired at the `07` **CALL SITE** (NOT inside `load_prices`, D1-F2) and the SAME windowed `actions` list feeds `load_prices` AND `compute()` so series/issue/terminal stay in sync (Task 4 Step 6); Task-8 patch set admits any ISIN-matched OR in-window symbol-added event; `corp_action_out_of_window` flag drops only out-of-window symbol-added attachments (of the 86 OOW union actions) into the dynamic unresolved set (Task 6) and is on the Task-11 checklist. Fixes the CAUSE (the envelope tripwire only catches the symptom) WITHOUT injecting the D1-F1 regression.
- **Flag carrier column (R5/F-7) — plumbed end-to-end:** `corp_action_out_of_window` / `corp_action_unresolved` / `corp_action_envelope_violation` are defined in `07.columns()` (Task 4) and reach `ipo_analysis.csv` via `09`'s `ret_keys` → `ret_cols` → `out_cols` (`09:41-45`) — so the flags Tasks 4/5/5b set are readable by Task 6 (not inert).
- **Re-review blockers/nits applied (R1–R5):** R1 — ROLEXRINGS detector + override re-keyed from `INE645S01016` to substrate `INE645S01024` (+ symbol) so the red baseline exercises the real ×1000 / +15,272% path (not a spurious gap=None→1.0 pass). R2 — detector helper, Task-4 reconcile grouping, override loader, unresolved set, and Task-8 patch/anti-join all key on the `actions_for` symbol∪substrate-ISIN union (per "corp actions match by SYMBOL"; blast radius 282 symbols / 434 rows / 0 corp-action ISINs with a price file), NOT the corp-action-file ISIN. R3 — duplicate Task-3 "Step 6" renumbered (contradiction guard = Step 6, commit = Step 7). R4 — ONE gap helper: Task 1's `corp_action_audit` reuses `pipeline.price_gap.detect_gap` (Task 2) instead of a second scanner. R5 — Task 8 spells out the `substrate_meta.json` fields that move (row count unchanged; `outcome_class` distribution, flagged-row tallies, `data_quality_tier` distribution).
- **R5 implementability fixes applied (final-implementability round):** F-7 — flag carrier column defined in `07.columns()` + plumbed via `09` `ret_keys` (Task 4 modify bullet; Tasks 5b/6 reference it). F-1 — Task 1 BUILD-ORDER note: build Task 2 (`price_gap`) before Task 1 Step 4/5 (or stub the import) so the green baseline is reachable. F-3 — `detect_gap` window is series-INDEX-adjacent, not calendar (Task 2 Step 3 + a hole-spanning test); the load-bearing detail behind ROLEXRINGS ~20× / KAUSHALYA ~100×. F-2 — `applied_effective` for a multi-event product = `max(P, 1/P)` (Task 1 helper). F-4 — ROLEXRINGS override seed pinned literally + quoted `csv.DictReader` (Task 3 Step 1). F-8 — Task 6 null-set enumerated explicitly (named constant the test imports).
- **R6 final-correctness fixes applied (whole-system gate):** D1-F1 (HIGH) — date-window gate scoped to the SYMBOL-ADDED subset ONLY; ISIN-matched actions always apply; 86 OOW union actions (44 was symbol-added only); ATLANTAA/TARIL/ADANIPORTS/TITAGARH/BAJAJCON regression guards in Task 4 Step 6 + Task 8 Step 2 (join-key block, Task 1, Task 4 Step 4, Task 6, Task 11 all re-scoped). D1-F2 (MED) — gate at the `07` CALL SITE (~599-600/~630-631), one windowed `actions` list to BOTH `load_prices` AND `compute()` so issue (438)/terminal (270) don't desync (Task 4 modify bullet + Step 6). D1-M1 (MED) — full anti-join compares the PRE-EXISTING column set only; new `corp_action_*` columns excluded + asserted empty on non-corp-action rows (Task 8 Steps 2/4, Task 11(d)). D1-N1 (NIT) — compound same-day split+bonus (ISHAN 30 vs gap 28.56) BOTH-legs-retained test (Task 4 Step 2 + Step 4 collapse caveat).
- **R8 adversarial / R9 remedy-(a) fold applied (the substrate-reaching enforcement):** R8 (HIGH, build-blocking) found "price disposes" never reached the substrate — the price-validating reconcile (`03k`/`03l`) is DAG-orphaned and `07` applied the window gate with NO gap check, so in-window phantoms / same-event double-counts (CANTABIL 25, GNA 4, RPEL/VISHWARAJ/GICL/PAVNAIND/KNRCON/ASIANTILES) survived with inflated multibagger magnitudes — invisible to the envelope tripwire (provably blind to a uniform over-count) and the override table (named seeds only). **Remedy (a) folded:** per-action price-validation at the `07` CALL SITE (new **Task 4 Step 6b** — gap-supports → apply; same-ratio legs → one gap → collapse-to-one; price-present-no-gap → PHANTOM-DROP + flag `corp_action_no_price_gap`; data-missing coverage-hole → defer to override / flag-unresolved), reusing the SHARED reconcile helper (Task 4 Step 4) so they can't drift, and inherited by the Task-8 patch via `07.compute()`. New flag `corp_action_no_price_gap` added to `07.columns()` (F-7 carrier) + the dynamic unresolved set (Task 6). Tests: CANTABIL/GNA added to the Task-1 detector parametrize (RED — squared factor) and the Task-8 substrate test (GREEN — collapsed 25→5 ~7.14× / 4→2 ~2.70×, NOT multibagger); Task-4 Step 2/6b cover apply / collapse-to-one / phantom-drop / coverage-hole-defer; the envelope-tripwire-blind uniform-over-count class is explicitly asserted caught by the price-validation (Task 11 g). **Coverage-hole-vs-phantom distinction preserved — ROLEXRINGS is NOT phantom-dropped (resolves via override seed).** The substrate-reaching enforcement is the `07`-path validation, NOT the orphaned `03k`/`03l` (stated in the Architecture line, the Task-4 DAG note, Task 4 Step 4/6b, Task 8 Step 3, Task 11). **Design unchanged** (price-disposes, flag+null, genuine-wipeout=−100%, confidence invariant, targeted-patch).
- **Prior review fixes (F1–F6 + nits) — carried, still applied:** F1 — direction-normalized detector (`applied_effective`, log-magnitude); PATANJALI removed everywhere, KAUSHALYA is the reverse-split must-pass; Task 9 Step 1 three-way. F2 — Task 5b permanent envelope tripwire (flag/raise, never clamp; TDD). F3 — Task 3 Step 6 override contradiction guard. F4 — NPST out of the Task-1 magnitude parametrize (caught by the count-conflict signal). F5 — Task 7 re-scoped to the MFE/MAE block (line 110 `mae_1y`), terminal (line 106) untouched. F6 — full anti-join gate (now pre-existing-column-set scoped, D1-M1) + `manual_overrides.csv` enumerated. Nits N1 (no-op snapshot commit removed), N2 ("skipped for corp-action stocks", line 89), N3 (Task 8 reuses `07`'s compute; `03k/03l` DAG-orphan + the `07` call-site gate IS the substrate-reaching part), N4 (trading-day window agreement Task 1↔Task 2 — now via the shared `detect_gap`), N5 (Task 9 verifies substrate delisting test line 103).
- **Line-ref corrections:** `07` `load_corp_actions` 91-111 + `actions_for` union 114-127; factor application = `load_prices` 206-235 + `adj_factor_after` 239-246 (called at 270 terminal + 438 issue→adj_issue); two call sites where actions+prices meet = ~599-600 (test) + ~630-631 (main); `07.columns()` = ~530-549 (new flag columns appended after `price_source`); `03k` dedup to replace = `load_yahoo_data` 75-91 (145-195 is the matching loop); `03l` union = line 58; `09` skip = `and not has_action` at line 89; `09` `ret_keys`/`out_cols` plumbing = 41-45; `07` MFE/MAE block = `_mfe_mae_block` 306-360.
- **Placeholders:** none — `_ex_dates` inline-implementation noted; all tests have real assertions/data.
- **Type consistency:** `detect_gap`→`Gap(ratio,direction,gap_date)` used consistently; `override_for(isin)→Override(canonical_factor,apply_once,direction)` consistent; `applied_cumulative_factor(isin)` + `applied_effective(isin)` + `observed_max_gap(isin)` shared between Task 1 and Task 9; `check_continuity`/`check_envelope` are flag-only.
- **Risk:** Task 8 Step-1 inventory is a hard gate — recompute must not clobber manual remediations (incl. `manual_overrides.csv`).
- **Design decisions unchanged** (per the review mandate): price-disposes, flag+null, genuine-wipeout=−100%, targeted-patch, confidence invariant — only defects fixed.
