"""foundation/ — the data-foundation rebuild (clean · extensible · correct).

New-namespace home for the rebuilt control plane: config (this package), the ingest honesty layer,
the column/rule registries, the overlay and golden files, and the generic assembly pipeline.
Built BESIDE the old pipeline/scrapers (which stay frozen until BL-3). See docs/research/FOUNDATION_*.md.
"""
