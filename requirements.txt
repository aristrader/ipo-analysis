# Pinned to the versions in the working .venv (DEPS-2, 2026-06-04).
# To refresh after a deliberate upgrade: pip freeze, then update the pin here.

# Core scraping
requests==2.34.2
cloudscraper==1.2.71
beautifulsoup4==4.14.3
lxml==6.1.1
curl_cffi==0.15.0

# Data / analysis
pandas==3.0.3
numpy==2.4.6
matplotlib==3.10.9

# App + verification
streamlit==1.58.0
playwright==1.60.0

# Financials recovery (DRHP PDFs)
pdfplumber==0.11.9

# Dev / tests
pytest==9.0.3

# Price history (installed; yahoo fallback scraper)
yfinance==1.4.1
